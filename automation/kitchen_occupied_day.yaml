alias: Kitchen Occupied Day
trigger:
  platform: state
  entity_id: binary_sensor.kitchen_occupied
  from: 'off'
  to: 'on'
condition:
  condition: state
  entity_id: 'input_boolean.night_mode'
  state: 'off'
action:
  service: homeassistant.turn_on
  data:
    entity_id: 
      - switch.switch
